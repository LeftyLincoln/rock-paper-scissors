
class Player {
  constructor(user, token) {
    this.name = user
    this.token = token
    this.wins = 0
    this.choice = choice
    

    
  }

  takeTurn(choice) {
     if (choice) {

     } 
  }


  randomGenerator() {

  }

}


function getRandomIndex(array) {
  return Math.floor(Math.random() * array.length);
}